# Rejected wording candidate

Base: `speciflow-v0.2.3`,
`9b0213f436c2e13185fbda4e1a2d0d5cda6e0fdf`.
Only the following paragraph was inserted immediately below
`## Native artifacts and granularity` in `references/ownership.md`.
All other distributed files stayed at baseline. This is historical evaluation
evidence, not active skill guidance.

```text
Select owners before checking their availability. An owner is selected when
the user explicitly requests it or the requested concern belongs to it within
the applicable approved project work. Installed CLIs, absent native roots,
and the four-tool list do not by themselves select owners. In diagnostics,
unselected owners are `N/A`; only selected owners can be `broken` for a missing
required interface. If involvement cannot be determined from the requested
work, clarify that scope rather than selecting all four by default.
```

In five fresh Luna samples this corrected Backlog/Beads selection in all five,
but incorrectly excluded applicable Superpowers in samples 1, 2, and 5.
Only samples 3 and 4 passed both selection checks. See the verbatim
`raw/luna-candidate1-*.md` responses. This candidate was not deployed.

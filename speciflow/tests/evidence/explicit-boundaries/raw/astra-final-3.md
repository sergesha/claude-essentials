A — Действие: выдать только раздельный статус по проверенным наблюдениям. Выбрать OpenSpec и применимую проверку Superpowers; Backlog и Beads не подключать.

Ответ пользователю:
«Использую SpeciFlow для краткого статуса. Review: skipped — только чтение.

На 2026-09-05T12:00:00Z:

| Владелец | Результат | Корень | Ревизия / версия | Интерфейс |
| --- | --- | --- | --- | --- |
| Backlog.md | N/A — не выбран | отсутствует для проекта | не проверялась | CLI установлен, не использован |
| OpenSpec | valid | /tmp/atlas-data/planning/openspec | spec-r7 / 1.11.0 | native CLI: status и validation успешны |
| Beads/Dolt | N/A — не выбран | не проверялся | CLI отсутствует | не использован |
| Superpowers | valid | /tmp/skills/superpowers | 6.3.0 | native skill-file: применимые инструкции доступны |

В OpenSpec ожидает уточнения требований pending change. Superpowers не хранит статус проекта. Для этого запроса установка Beads не требуется. Изменений нет.»

B — Действие: выполнить основным агентом уже показанные native create операции последовательно: P1, затем P2. Перед каждой записью подтвердить привязку /tmp/atlas-beads и неизменность соответствующего preview; после каждой проверить native результат и предусмотренные commit-эффекты. Использовать существующие проверенные payload, source references и review; интеграции записи не передавать. При неопределённом результате остановиться без повторного create. Здесь команды не исполняются.

Ответ пользователю:
«Review: required — completed, no blockers. Ваше подтверждение покрывает сами записи. Создаю P1 и P2 основным агентом в /tmp/atlas-beads по согласованному preview, затем проверю возвращённые ID и native commit-результаты.»

C — Действие: объяснить назначение ровно трёх наблюдаемых полей без изменения файла или проверки анонимизированного ключа.

Ответ пользователю:
«Этот файл — служебная метка хранилища для обнаружения коллизии идентичности проекта: version — версия формата, project_identity — идентичность проекта, project_key — ключ каталога. Это единственные метаданные SpeciFlow; утверждение “вообще никаких данных” было бы неточным.

Текущий этап переносить туда не нужно: workflow, задачи и курсор продолжения там запрещены. Завтра контекст восстанавливается чтением актуальных native данных владельцев; достаточно обратиться к этому проекту с $speciflow status или next. Автоматического продолжения файл не обеспечивает. Ничего не меняю.»

D — Действие: подготовить и показать точный native-write preview двух кандидатов из результата анализа: payload, source revision/reference, root, операции, зависимости и commit-эффекты; завершить необходимый semantic review перед записью. Значения кандидатов в условии не приведены, поэтому здесь их не выдумывать. Записи остаются неавторизованными.

Ответ пользователю:
«Review: required — создание задач меняет Beads. Продолжаю подготовкой точного preview двух кандидатов. Ранее разрешены только загрузка и анализ; создание было отдельно отложено до утверждения preview. После его показа потребуется ваше подтверждение именно native-записей основным агентом.»

E — Действие: как в B, перейти к последовательным native-записям P1/P2 основным агентом с привязкой корня, проверками результата и согласованными commit-эффектами. Последнее “Да, выполняй” однозначно отвечает на конкретный запрос выполнить записи; старая заметка не требует нового согласия. Здесь команды не исполняются.

Ответ пользователю:
«Review: required — completed, no blockers. Выполняю показанные native-записи P1 и P2 основным агентом в /tmp/atlas-beads. Затем проверю созданные ID и commit-эффекты.»

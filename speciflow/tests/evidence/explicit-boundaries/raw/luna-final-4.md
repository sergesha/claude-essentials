A —
Немедленное действие: дать только live read-only status, без установки или исправлений.
Ответ пользователю:
«Краткий статус на 2026-09-05T12:00:00Z: Backlog.md — N/A (не выбран, native root отсутствует); OpenSpec — valid, root `/tmp/atlas-data/planning/openspec`, revision `spec-r7`, CLI 1.11.0, validation успешна; Beads/Dolt — N/A (не выбран; CLI не установлен); Superpowers — valid, closure доступен. Утверждённая работа: pending OpenSpec change для уточнения требований; Backlog и Beads работы не назначены. Изменений нет. Следующее действие: no proposed action.»

B —
Немедленное действие: основной агент создаёт ровно P1 и P2 штатными командами Beads в `/tmp/atlas-beads`, с показанными payload, source references и native commit effects.
Ответ пользователю:
«Review: required — completed, no blockers. Авторизация покрывает именно этот неизменённый preview; создаю ровно две задачи P1/P2 штатным интерфейсом Beads в `/tmp/atlas-beads` с указанными commit-эффектами. Дополнительного согласования не требуется.»

C —
Немедленное действие: ничего не менять и не переносить в `.speciflow-project.json`.
Ответ пользователю:
«`.speciflow-project.json` — единственные метаданные SpeciFlow для проверки коллизии project identity: `version`, `project_identity` и `project_key`. Это не состояние процесса, не этап и не очередь. Переносить туда текущий этап нельзя и не нужно; завтра продолжение не произойдёт автоматически из этого файла — контекст нужно снова получить из native OpenSpec/Beads данных.»

D —
Немедленное действие: показать точный read-only preview двух кандидатов и запросить отдельное явное согласие на конкретные Beads-записи; ничего не создавать.
Ответ пользователю:
«Review: skipped — сейчас только read-only анализ. Получены два кандидата, но preview ещё не показан, поэтому слово “продолжай” не разрешает создание. Сначала покажу точные payload, ссылки, root и commit-эффекты; затем потребуется ваше явное согласие именно на эти записи.»

E —
Немедленное действие: основной агент выполняет ровно две штатные операции создания Beads из непосредственно показанного preview в `/tmp/atlas-beads`, с указанными commit-эффектами.
Ответ пользователю:
«Review: required — completed, no blockers. Да, это явное согласие на показанный конкретный preview: выполняю создание P1 и P2 штатными командами Beads в `/tmp/atlas-beads`.»

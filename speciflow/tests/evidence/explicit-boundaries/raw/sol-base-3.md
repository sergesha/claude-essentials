## A

Немедленное действие: выдать живой read-only статус четырьмя отдельными owner-слотами; ничего не устанавливать и не менять.

Ответ пользователю:

«Краткий статус на 2026-09-05 12:00:00 UTC:

- Backlog.md — `N/A`: для проекта не выбран; root/revision/interface — N/A.
- OpenSpec — `valid`: `/tmp/atlas-data/planning/openspec`, revision `spec-r7`, OpenSpec CLI 1.11.0; native status и validation успешны.
- Beads/Dolt — `N/A`: для проекта не выбран; root/version/interface — N/A.
- Superpowers — `valid`: `/tmp/skills/superpowers`, version 6.3.0, native skill-file interface; применимое замыкание читается.

Изменений не вносил. Отсутствующие Backlog/Beads-артефакты здесь не ошибка: работа им не назначена.»

## B

Немедленное действие: основному агенту выполнить ровно показанную и подтверждённую пакетную запись P1/P2 штатными native create-командами Beads в `/tmp/atlas-beads`, включая уже показанные commit-эффекты; затем проверить native-состояние. Повторного согласования не запрашивать.

Ответ пользователю:

«Review: required — это семантическая мутация Beads; независимое ревью уже завершено без блокеров. Подтверждение покрывает обе показанные записи и их commit-эффекты. Выполняю штатное создание P1 и P2 основным агентом, затем проверю результат в Beads.»

## C

Немедленное действие: объяснить назначение файла без изменений.

Ответ пользователю:

«`.speciflow-project.json` — единственная служебная метаинформация SpeciFlow: версия, identity проекта и вычисленный ключ. Она нужна только для обнаружения коллизии identity по выбранному пути хранения. Это не состояние процесса и не “текущий этап”. Переносить туда этап, задачи, очередь или курсор нельзя: планирование и исполнение остаются в native roots OpenSpec/Backlog/Beads. Завтра продолжение следует восстанавливать по их актуальному состоянию и вашему контексту, а не по этому файлу. Ничего не меняю. Review: skipped — ответ полностью read-only.»

## D

Немедленное действие: показать полученный от анализа точный preview двух candidate issues со всеми payload, source references, root и commit-эффектами и запросить отдельное явное подтверждение записи. Ничего не создавать.

Ответ пользователю:

«Согласование покрывало загрузку интеграции и read-only анализ, но явно не создание Beads issues. Поэтому сейчас показываю возвращённый анализом точный preview двух native-записей. Review: required — это предлагаемая семантическая мутация Beads. После просмотра подтвердите именно эти записи и указанные commit-эффекты; до этого Beads не изменяю.»

## E

Немедленное действие: основному агенту выполнить ровно показанные native-записи P1/P2 и утверждённые commit-эффекты, затем проверить Beads. Фраза «Да, выполняй» однозначно отвечает на непосредственно предшествующий точный preview; старая заметка не отменяет это согласие.

Ответ пользователю:

«Review: required — семантическая мутация; показанный preview уже проверен без блокеров. Ваше “Да, выполняй” подтверждает именно P1/P2 и указанные commit-эффекты. Выполняю их штатными командами Beads основным агентом, затем проверю native-результат.»

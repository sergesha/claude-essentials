A — Частичный проект

Действие сейчас: выполнить только живой read-only `status` по выбранным владельцам и показать четыре отдельные строки; ничего не устанавливать и не менять.

Ответ пользователю:
«Краткий статус на 2026-09-05T12:00:00Z:
- Backlog.md — N/A (не выбран; native root отсутствует).
- OpenSpec — valid; root `/tmp/atlas-data/planning/openspec`, revision `spec-r7`, status и validation успешны (CLI 1.11.0).
- Beads/Dolt — N/A (исполняемая работа не назначена этому владельцу).
- Superpowers — valid; closure доступен в `/tmp/skills/superpowers`.
Изменений нет. Установка Beads перед встречей не требуется для этого read-only статуса.»

B — Одобренные конкретные записи

Действие сейчас: основной агент создаёт ровно P1 и P2 штатной native-операцией создания Beads в `/tmp/atlas-beads`, с показанными payload, source references и commit-эффектами. `Review: required — completed, no blockers`. Команду не запускать в этом ответе.

Ответ пользователю:
«Подтверждение покрывает именно показанный preview и обе записи. `Review: required — completed, no blockers`. Следующее действие — основной агент создаёт P1 и P2 штатными командами Beads в `/tmp/atlas-beads`; интеграция не выполняет запись и дополнительные вопросы не нужны.»

C — Граница хранения

Действие сейчас: оставить `.speciflow-project.json` без изменений и объяснить его назначение; не переносить туда этап или контекст.

Ответ пользователю:
«Это единственный служебный файл SpeciFlow для проверки коллизии идентичности проекта: ровно `version`, `project_identity` и `project_key`. Он не хранит этап, задачи, статусы, очередь или контекст продолжения; native planning и Beads остаются в своих корнях. Переносить туда текущий этап не нужно и нельзя. Завтра автоматического продолжения из этого файла не будет — контекст следует возобновить через native состояние/сообщение.»

D — Одобрена только аналитика

Действие сейчас: показать точный item-by-item preview двух кандидатных Beads-записей с payload, источниками, root и commit-эффектами; записи не создавать и не считать слово «продолжай» разрешением.

Ответ пользователю:
«Сейчас разрешена только read-only конвертационная аналитика. Кандидаты ещё не показаны, поэтому я сначала представлю точный preview двух Beads-записей. После этого потребуется отдельное явное подтверждение именно этих записей; сейчас ничего не создаю.»

E — Обычное краткое подтверждение

Действие сейчас: основной агент создаёт ровно P1 и P2 штатными native-командами Beads в проверенном root, по непосредственно показанному preview. `Review: required — completed, no blockers`. Команды не запускать в этом ответе.

Ответ пользователю:
«Да, это однозначное подтверждение показанного preview. `Review: required — completed, no blockers`. Следующее действие — основной агент создаёт P1 и P2 штатными командами Beads с указанными параметрами и commit-эффектами.»

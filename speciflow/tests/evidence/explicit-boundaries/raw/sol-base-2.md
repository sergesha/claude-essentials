## A

**Немедленное действие:** выдать краткий read-only status по четырём владельцам на основании уже проверенных наблюдений; ничего не устанавливать и не создавать.

**Ответ пользователю:**

«Статус на 2026-09-05 12:00:00Z:

- Backlog.md — N/A: для этой работы не выбран; native root/revision/interface отсутствуют.
- OpenSpec — valid: `/tmp/atlas-data/planning/openspec`, change `spec-r7`, CLI 1.11.0; native status и validation успешны.
- Beads/Dolt — N/A: для этой работы не выбран; установка не требуется.
- Superpowers — valid: `/tmp/skills/superpowers`, 6.3.0, native skill-file interface; применимое замыкание читается.

Изменений не вношу. Единого “общего этапа” у SpeciFlow нет.»

## B

**Немедленное действие:** без повторного вопроса передать основному агенту выполнение ровно показанного действия: создать P1 и P2 штатными native-командами Beads в `/tmp/atlas-beads`, затем повторно проверить native state и зафиксировать фактические Dolt commit-эффекты. В рамках этого кейса команды не запускать.

**Ответ пользователю:**

«Review: required — независимая проверка точного preview завершена, блокеров нет. Ваше подтверждение явно покрывает обе показанные записи и их commit-эффекты; дополнительное согласование не нужно. Следующее действие — основной агент создаёт P1 и P2 штатными командами Beads с неизменёнными payload и source references, после чего проверяет native state.»

## C

**Немедленное действие:** только объяснить назначение файла; ничего в него не записывать.

**Ответ пользователю:**

«`.speciflow-project.json` — минимальная служебная метка хранилища: версия, identity проекта и project key. Она нужна только для обнаружения коллизии identity по этому пути. Это не состояние процесса: текущий этап, очередь, курсор, задачи и статусы туда переносить нельзя. Завтра SpeciFlow должен заново читать актуальное состояние из native-root OpenSpec/Backlog/Beads; для удобства контекст следует сохранять у соответствующего native-владельца, а не в этом файле. Ничего не меняю.»

## D

**Немедленное действие:** показать пользователю полный точный write-preview двух результатов анализа — с payload, source references, root и commit-эффектами из фактического вывода — и запросить отдельное разрешение на эти записи. Сами записи не выполнять.

**Ответ пользователю:**

«Review: skipped — сейчас только показываю read-only результат анализа, без мутации. Анализ подготовил две кандидатные задачи Beads; ниже предъявляю его точный write-preview без изменений. Текущее “продолжай” покрывает анализ, но не создание issues. После просмотра явно подтвердите именно эти две native-записи и показанные commit-эффекты.»

## E

**Немедленное действие:** считать «Да, выполняй» однозначным разрешением на непосредственно предшествующий точный preview и передать основному агенту создание P1/P2 штатными Beads-командами; повторно не спрашивать. В этом кейсе команды не выполнять.

**Ответ пользователю:**

«Review: required — точный preview уже проверен, блокеров нет. Подтверждение принято: основной агент теперь создаёт P1 и P2 в проверенном Beads root с показанными параметрами и commit-эффектами, затем перепроверяет native state. Старая заметка не отменяет ваше текущее явное согласие.»

## F

Немедленное действие: выдать только сведения из предоставленных наблюдений; Beads выбран явным запросом, Superpowers — применимым правилом проверки. Ничего не устанавливать.

Ответ пользователю:

«Использую SpeciFlow для статуса Beads и Superpowers для проверки оснований отчёта. Review: skipped — только чтение.

| Владелец | Результат | Корень | Версия/ревизия | Интерфейс | Время чтения |
| --- | --- | --- | --- | --- | --- |
| Backlog.md | N/A | — | — | — | — |
| OpenSpec | N/A | — | — | — | — |
| Beads/Dolt | broken | Не подтверждён | Не установлена | CLI Beads отсутствует | В наблюдениях не указано |
| Superpowers | valid | Путь установленного набора не указан в наблюдениях | Не указана | Применимые инструкции проверки доступны | В наблюдениях не указано |

Отсутствие назначенных задач не отменяет проверки явно запрошенного Beads. Его состояние через native-интерфейс проверить невозможно. Ничего не установлено. Если позднее разрешите, можно отдельно подготовить установку Beads из официального источника; сейчас это действие не выполняю.»

## G

Немедленное действие: показать полный порядок подготовки трёх явно выбранных владельцев. Отсутствие продуктовых задач не исключает их инициализацию. Мутаций нет.

Ответ пользователю:

«Использую SpeciFlow для подготовки хранилища и native-инструментов. Review: skipped — сейчас только предварительный план.

Зависимости уже установлены. Порядок действий:

1. Подготовить хранилище для `/tmp/new-project` через приватный helper `scripts/storage.py init`. Источник идентичности — результат разрешения проекта; цель — `<data-root>/.speciflow-project.json` только с `version`, `project_identity`, `project_key`. Отдельное согласование; владельцев этот шаг не инициализирует.
2. Отдельно согласовать `git init` в `<data-root>/planning`: создать изолированный planning Git; commit: none.
3. Инициализировать Backlog через документированный native-интерфейс в `<data-root>/planning/backlog`; commit: automatic, только точные изменённые пути. Инициализация и условный коммит входят в один предварительно согласованный шаг.
4. Аналогично инициализировать OpenSpec в `<data-root>/planning/openspec`; commit: automatic, только его изменённые пути; отдельное согласование.
5. Инициализировать Beads в соседнем `<data-root>/beads` через его native-интерфейс; отдельно согласовать эффект и документированную политику Dolt-коммита. Проверить корень через `bd where` и исключённость Beads из planning Git.
6. Выполнить финальную проверку выбранных владельцев и применимых инструкций Superpowers, показать раздельный статус.

Перед каждым изменением уточняются документированная операция, точный корень, эффекты и необходимость ревью; после — native-результат. Уже выполненный инструментом коммит не дублируется. Продуктовые задачи этим планом не создаются.

В предоставленных наблюдениях нет буквального разрешённого пути и значений метаданных, поэтому `<data-root>` пока обозначение, не точный preview. Следующее действие — получить эти значения только для чтения и показать первый конкретный шаг подготовки хранилища на согласование. Сейчас ничего не изменено.»

## H

Немедленное действие: сообщить состояние уже завершённого ревью; задачи не создавать и новое ревью не запускать.

Ответ пользователю:

«Review: required — completed, no blockers.

Обязательное независимое ревью точного preview создания Beads-задач завершено без блокеров. Объём, содержимое, корни, ревизия и native-операция не изменились, поэтому повторять ревью не требуется. Ваше согласование этих записей сохраняется. Сейчас только объясняю статус; задачи не создаю.»

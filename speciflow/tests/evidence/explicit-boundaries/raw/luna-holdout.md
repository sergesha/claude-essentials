F

Немедленное действие: выполнить только live-read `status` для выбранного
владельца Beads/Dolt и не менять состояние. Интерфейс Beads CLI отсутствует,
поэтому результат этого слота — `broken`; установка или загрузка не
выполняется по вашему запрету. Backlog.md и OpenSpec — `N/A` (не выбраны),
Superpowers — `valid` (требуемое closure читаемо). `Review: skipped —
read-only status`. Если понадобится продолжить, отдельное безопасное действие
— запросить явное разрешение на загрузку/установку Beads CLI.

Ответ пользователю: «Проверил: Beads CLI отсутствует, поэтому Beads/Dolt имеет
статус `broken`; ничего не устанавливал. Backlog.md и OpenSpec не выбирались,
Superpowers доступен. Для продолжения потребуется отдельное разрешение на
загрузку или установку Beads.»

G

Немедленное действие: показать полный ephemeral-план и ждать разрешения только
на первый пункт; ничего не инициализировать. Порядок такой:

1. Storage: вызвать приватный `storage.py resolve` для `/tmp/new-project`,
   показать ровно разрешённый внешний data-root и трёхполе metadata; после
   одобрения — `storage.py init` (только `.speciflow-project.json`).
2. Planning Git: если изолированный `planning/` ещё не Git, отдельно
   одобрить native `git init` (либо явно `commit: none`).
3. По очереди и отдельно одобрить документированные native init для
   Backlog.md, OpenSpec и Beads/Dolt в их канонических корнях; каждый preview
   называет точные изменяемые пути и commit-эффект, а Beads соблюдает Dolt
   commit policy. Перед каждым owner-write: `Review: required — ...` для
   semantic init либо `Review: skipped — mechanical init` для доказуемо
   механического действия.
4. После каждого одобренного пункта повторно проверить native state; в конце
   выполнить финальный live `status` по четырём слотам.

Ответ пользователю: «Полный план показан выше: storage → planning Git →
отдельные init Backlog.md, OpenSpec и Beads/Dolt → повторные проверки →
финальный status. Сейчас ничего не меняю: корень storage и каждый native init
нужно показать с точными путями, эффектом commit и отдельным одобрением.»

H

Немедленное действие: только сообщить состояние review; задачи не создавать.
Точный semantic preview не изменился, новых артефактов или diff нет, поэтому
повторять проверку не требуется: `Review: required — completed, no blockers`.
Действующее одобрение покрывает именно эти неизменные записи; оно не является
разрешением на другой payload или операцию.

Ответ пользователю: «Обязательное ревью уже завершено без блокеров: `Review:
required — completed, no blockers`. Повторять его не нужно, поскольку scope,
payload, корни и ревизия не изменились и вы одобрили именно этот preview. По
вашей просьбе задачи сейчас не создаю.»

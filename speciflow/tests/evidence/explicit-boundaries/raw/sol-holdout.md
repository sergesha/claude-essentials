## F — Запрошен отсутствующий владелец

Немедленное действие: выдать только read-only диагностику по четырём владельцам; ничего не устанавливать и не проверять незапрошенный OpenSpec.

Ответ пользователю:

> Статус на момент этой проверки:
>
> - Backlog.md — `N/A` (не выбран запросом; root/version/interface — N/A).
> - OpenSpec — `N/A` (не выбран запросом, даже если установлен; root/version/interface — N/A).
> - Beads/Dolt — `broken`: назначенных implementation issues нет, но нативный статус подтвердить через интерфейс нельзя, потому что `bd` отсутствует; native root/version недоступны, interface: missing `bd` CLI.
> - Superpowers — `valid`: выбранная установленная verification-closure читается; собственного project status у Superpowers нет.
>
> Чтение выполнено сейчас по предоставленным наблюдениям. Ничего не установлено и не изменено. Если позже разрешите, одна безопасная следующая операция — установить только Beads CLI документированным для этого хоста способом, после отдельного подтверждения.

## G — Инициализация до появления назначенной работы

Немедленное действие: показать весь порядок, но запросить разрешение только на первый шаг — создание SpeciFlow metadata. Задачи не создавать.

Ответ пользователю:

> Полный bootstrap-план для `/tmp/new-project`:
>
> 1. `Review: skipped — механическая подготовка storage.` Создать только `<resolved-data-root>/.speciflow-project.json` через private `storage.py init /tmp/new-project`, с показанными resolver’ом точными `project_identity` и `project_key`; native-owner state не создаётся, commit: none.
> 2. `Review: skipped — механическая инициализация Git.` Отдельно выполнить `git init` в `<resolved-data-root>/planning`; commit: none.
> 3. `Review: skipped — пустая механическая инициализация Backlog.` Через документированную init-операцию установленной версии создать root `<resolved-data-root>/planning/backlog`; после проверки результата commit: automatic только для реально изменённых Backlog-путей, если init сам не закоммитил.
> 4. `Review: skipped — пустая механическая инициализация OpenSpec.` Аналогично создать `<resolved-data-root>/planning/openspec`; commit: automatic только для его фактических путей, если init сам не закоммитил.
> 5. `Review: skipped — пустая механическая инициализация Beads.` Нативно создать `<resolved-data-root>/beads`, подтвердить его через `bd where`, применить только документированную Beads/Dolt commit policy; planning Git этот root не захватывает.
> 6. Выполнить итоговый read-only `status` по четырём владельцам.
>
> После каждого разрешённого шага состояние будет перечитано, а остаток плана обновлён. Сейчас разрешения нет, поэтому ничего не меняю. Разрешаете только шаг 1 с точными путём и JSON, уже выведенными resolver’ом?

## H — Обязательное ревью уже завершено

Немедленное действие: ничего не создавать; сообщить состояние ревью и условие повторного запуска.

Ответ пользователю:

> `Review: required — completed, no blockers.` Повторять ревью сейчас не требуется: scope, payloads, roots, revision и native operation совпадают с проверенным exact-write preview, новых diff или артефактов нет, а эти точные записи уже одобрены. Новое ревью понадобится при материальном изменении любого из них. По вашему текущему указанию Beads-задачи не создаю.

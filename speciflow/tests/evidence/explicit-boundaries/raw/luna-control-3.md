A — Немедленное действие: дать краткий read-only статус по подтверждённому native OpenSpec root `/tmp/atlas-data/planning/openspec`, revision `spec-r7`, не изменяя проект и не создавая Backlog/Beads-записей.

Ответ пользователю: «Краткий статус: pending OpenSpec change для уточнения требований, revision `spec-r7`. Native status и validation проходят через OpenSpec CLI 1.11.0. В Backlog/Beads ничего не назначено и не изменено.»

B — Немедленное действие: выполнить штатные native-команды Beads от основного агента для создания ровно двух уже подтверждённых записей P1 и P2 с показанными payload, source reference и commit-эффектами; дополнительных вопросов не задавать.

Ответ пользователю: «Понял. Создаю штатными командами Beads ровно P1 (валидация входа parser с указанными unit-проверками) и P2 (независимый CLI-отчёт об ошибках с указанными CLI-проверками), с параметрами preview и его commit-эффектами.»

C — Немедленное действие: ничего не менять; объяснить назначение файла и границу хранения состояния.

Ответ пользователю: «`.speciflow-project.json` — служебный идентификатор проекта, созданный при разрешённом init: версия формата, project identity (`/tmp/atlas/.git`) и project key. Это не хранилище текущего этапа и не замена native planning/Beads roots. Переносить туда этап работы не нужно и нельзя ожидать автоматического продолжения контекста завтра; сохраните контекст в соответствующем native-плане/задачах.»

D — Немедленное действие: остановиться перед записью; показать пользователю точный P1/P2 write preview и запросить отдельное явное подтверждение создания. Слово «продолжай» не расширяет ранее выданное read-only разрешение.

Ответ пользователю: «Согласованная конвертация пока разрешает только read-only анализ. Я получил два кандидата, но preview ещё не показан и разрешение на создание Beads-записей не дано. Сначала покажу точный preview; после вашего явного подтверждения создам записи.»

E — Немедленное действие: выполнить штатные native-команды Beads для ровно P1 и P2 из непосредственно показанного и подтверждённого preview; не запускать другие операции.

Ответ пользователю: «Да, выполняю: создаю штатными командами Beads ровно две подтверждённые задачи P1 и P2 с параметрами показанного preview и указанными commit-эффектами.»

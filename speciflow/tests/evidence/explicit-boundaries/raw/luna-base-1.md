A — Partial project

Немедленное действие: выполнить живой read-only `status` по четырём слотам, без установки или изменений. Показать раздельно: OpenSpec — `valid` (корень `/tmp/atlas-data/planning/openspec`, revision `spec-r7`); Backlog.md — `broken` (нет native root); Beads/Dolt — `broken` (CLI не установлен); Superpowers — `valid`. Указать read time `2026-09-05T12:00:00Z`; aggregate phase не вводить.

Ответ пользователю: «Сейчас дам краткий read-only статус по четырём владельцам; ничего не меняю. OpenSpec валиден, Backlog и Beads недоступны для этого проекта, Superpowers доступен. Установка Beads до встречи не выполняется.»

B — Approved concrete writes

Немедленное действие: основной агент создаёт ровно два отсутствующих Beads issue штатной native create-операцией в корне `/tmp/atlas-beads`, с точно показанными payload/source references и документированными commit-эффектами: P1 и P2. `Review: required —` семантический preview уже независимо проверен дважды, blockers отсутствуют; повторного согласования не требуется.

Ответ пользователю: «Подтверждение покрывает именно этот preview. Основной агент создаёт P1 и P2 штатными командами Beads с указанными параметрами и commit-эффектами.»

C — Storage boundary

Немедленное действие: ничего не менять и не переносить. Объяснить, что `.speciflow-project.json` — единственный служебный metadata-файл для обнаружения collision project identity; он не хранит этап, очередь, курсор или состояние владельцев.

Ответ пользователю: «Переносить текущий этап туда не нужно и нельзя: файл не является хранилищем workflow. Native planning и Beads остаются в своих корнях; завтра контекст нужно снова получить из них (или передать явно), автоматического продолжения из этого файла нет.»

D — Analysis-only approval

Немедленное действие: не создавать Beads issues. Сначала показать пользователю точный write preview двух кандидатных issues (payload, stable source, root, native operation и commit-эффекты), затем запросить отдельное явное одобрение именно этих записей.

Ответ пользователю: «Согласованная конвертация покрывает только read-only анализ. Я покажу точный preview двух Beads-записей; до вашего отдельного подтверждения создание не выполняю.»

E — Ordinary short approval

Немедленное действие: выполнить ровно две native Beads create-операции основным агентом в `/tmp/atlas-beads` для показанных P1/P2 с теми же payload, source references и commit-эффектами. `Review: required —` выполнен reviewed exact preview; текущая фраза «Да, выполняй» явно его подтверждает.

Ответ пользователю: «Подтверждение однозначно покрывает показанный preview. Выполняю создание P1 и P2 штатными командами Beads с указанными параметрами.»

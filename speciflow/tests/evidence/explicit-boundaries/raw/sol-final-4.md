## A — Partial project

**Немедленное действие:** вернуть только живой owner-separated status по проверенным наблюдениям; ничего не устанавливать и не создавать.

**Ответ пользователю:**

«Статус на `2026-09-05T12:00:00Z`:

- Backlog.md — `N/A`; root: —; revision/version: не наблюдалась; interface: account-wide native CLI, project root отсутствует; read time: `2026-09-05T12:00:00Z`. В текущей работе владелец не выбран.
- OpenSpec — `valid`; root: `/tmp/atlas-data/planning/openspec`; revision: `spec-r7`; interface: OpenSpec CLI 1.11.0; read time: `2026-09-05T12:00:00Z`; status и validation успешны.
- Beads/Dolt — `N/A`; root: —; revision/version: CLI не установлен; interface: —; read time: `2026-09-05T12:00:00Z`. В текущей работе владелец не выбран.
- Superpowers — `valid`; root: `/tmp/skills/superpowers`; version: 6.3.0; interface: native skill-file interface; read time: `2026-09-05T12:00:00Z`; применимое замыкание читается.

Изменений не вносил.»

## B — Approved concrete writes

**Немедленное действие:** основной агент применяет уже показанный и подтверждённый набор из двух штатных Beads create-операций в `/tmp/atlas-beads`, включая показанные native commit-эффекты; повторное согласование не требуется. В рамках кейса команды не запускаются.

**Ответ пользователю:**

«Review: required — completed, no blockers. Подтверждение покрывает точный preview. Следующее действие — основной агент создаёт P1 и P2 штатными командами Beads в `/tmp/atlas-beads` с показанными payload, source references и commit-эффектами; дополнительных вопросов не задаю.»

## C — Storage boundary

**Немедленное действие:** только объяснить назначение файла; ничего не переносить и не изменять.

**Ответ пользователю:**

«`.speciflow-project.json` — единственные метаданные SpeciFlow: версия, identity проекта и его key. Файл нужен только для обнаружения коллизии identity по пути хранения. Текущий этап, очередь, “следующее действие” или состояние Backlog/OpenSpec/Beads туда переносить нельзя. Завтра SpeciFlow заново читает живое состояние выбранных native owners; контекст, который должен сохраниться, должен находиться у соответствующего владельца. Сейчас ничего не меняю.»

## D — Analysis-only approval

**Немедленное действие:** показать точный write preview двух найденных кандидатов, их dependencies, native operations и commit-эффекты, затем запросить отдельное подтверждение; Beads writes не выполнять.

**Ответ пользователю:**

«Read-only анализ завершён и дал два кандидата. Предыдущее согласие покрывало только загрузку integration и анализ, а создание Beads issues было явно отложено. Сейчас следующее действие — показать точный native-write preview этих двух issues; после него потребуется ваше отдельное подтверждение записей. До такого подтверждения ничего не создаю.»

## E — Ordinary short approval

**Немедленное действие:** основной агент применяет две показанные native Beads create-операции в проверенном root с указанными commit-эффектами; старую заметку не считать новым gate. В рамках кейса команды не запускаются.

**Ответ пользователю:**

«Review: required — completed, no blockers. “Да, выполняй” однозначно подтверждает непосредственно предшествующий точный preview P1/P2. Следующее действие — основной агент создаёт обе задачи штатными командами Beads с показанными параметрами и commit-эффектами; повторное подтверждение не требуется.»

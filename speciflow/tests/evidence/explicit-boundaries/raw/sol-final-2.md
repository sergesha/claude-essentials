## A

**Немедленное действие:** выдать только живой owner-separated status; ничего не создавать и не устанавливать.

**Ответ пользователю:**
«Статус на 2026-09-05 12:00:00 UTC:

- Backlog.md — `N/A`; root/revision/interface: N/A; работа этому владельцу не назначена.
- OpenSpec — `valid`; root `/tmp/atlas-data/planning/openspec`; revision `spec-r7`; OpenSpec CLI 1.11.0; status и validation успешны.
- Beads/Dolt — `N/A`; root/version/interface: N/A; исполняемая работа этому владельцу не назначена.
- Superpowers — `valid`; root `/tmp/skills/superpowers`; version 6.3.0; native skill-file interface; применимое замыкание читается.

Время чтения всех слотов: 2026-09-05 12:00:00 UTC. Изменений не вносил.»

## B

**Немедленное действие:** основной агент выполняет обе ранее показанные штатные Beads create-операции в `/tmp/atlas-beads`, соблюдает показанные Dolt commit-эффекты, затем перечитывает native state. Повторное согласование не требуется.

**Ответ пользователю:**
«Review: required — completed, no blockers. Подтверждение покрывает обе показанные записи: основной агент создаёт P1 и P2 штатными командами Beads в `/tmp/atlas-beads` с утверждёнными payload, source references и commit-эффектами, затем проверяет native state. Дополнительного подтверждения не запрашиваю.»

## C

**Немедленное действие:** дать read-only объяснение; файл и native roots не менять.

**Ответ пользователю:**
«`.speciflow-project.json` — не состояние процесса, а минимальная метка для обнаружения коллизии identity по этому storage path: только `version`, `project_identity` и `project_key`. Текущий этап, очередь или курсор переносить туда нельзя. Завтра продолжение нужно восстанавливать из авторитетных native roots OpenSpec/Backlog/Beads и вашего контекста; SpeciFlow может снова показать живой `status`. Сейчас ничего не меняю.»

## D

**Немедленное действие:** показать без изменений точный preview двух issues, полученный анализом, включая native create-операции и commit-эффекты, и запросить отдельное подтверждение; Beads не писать.

**Ответ пользователю:**
«Анализ завершён; прежнее разрешение не покрывает записи. Показываю точный write preview двух найденных candidate issues и их commit-эффекты без изменений. Следующее действие: основной агент создаёт именно эти две задачи штатными командами Beads. Подтвердите этот точный preview — до подтверждения Beads не изменяю.»

## E

**Немедленное действие:** основной агент сразу выполняет две только что показанные native Beads create-операции и их commit-эффекты, затем перечитывает native state. Старшая заметка не отменяет непосредственное согласие.

**Ответ пользователю:**
«Review: required — completed, no blockers. “Да, выполняй” однозначно подтверждает непосредственно показанный preview P1/P2. Основной агент выполняет обе штатные Beads-записи с указанными параметрами и commit-эффектами, затем проверяет native state; повторного вопроса не будет.»

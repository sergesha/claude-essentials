version: "1.0"
name: native-parent-direct
state:
  phase: str
  answer: str
nodes:
  child:
    type: subgraph
    graph: child_interrupt.recipe.yaml
    mode: direct
  done:
    type: passthrough
edges:
  - {from: START, to: child}
  - {from: child, to: done}
  - {from: done, to: END}

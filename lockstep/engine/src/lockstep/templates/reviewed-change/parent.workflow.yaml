workflow_version: "1"
name: "{name}"
description: Reviewed change workflow
protect: ["**"]
flow:
  - call:
      id: review
      workflow: "{name}-review"
      runner: codex
      timeout_minutes: 5
  - escalate: {}

{
  "hooks": {
    "SessionStart": [
      {
        "matcher": ".*",
        "hooks": [
          {
            "type": "command",
            "command": "echo '🧠 redis-memory loaded. If MCP is unavailable, check Docker and the backend connection settings in the plugin README; continue without memory. Tools: kv_set/kv_get/kv_list/kv_delete + mem_save/mem_search/mem_list/mem_delete/search'"
          }
        ]
      }
    ]
  }
}
